var BABYLON;
(function (BABYLON) {
    var Main = /** @class */ (function () {
        // Constructor
        function Main(scene) {
            this.scene = scene;
        }
        // Create collisions on camera
        Main.prototype.setupCollisions = function () {
        };
        // Create some actions
        Main.prototype.createActions = function () {
        };
        // Setup physics and create some bodies
        Main.prototype.setupPhysics = function () {
        };
        return Main;
    }());
    BABYLON.Main = Main;
})(BABYLON || (BABYLON = {}));
//# sourceMappingURL=main.js.map